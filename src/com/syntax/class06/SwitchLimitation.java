package com.syntax.class06;

public class SwitchLimitation {

	public static void main(String[] args) {
		//switch can work with byte short int char string
		// switch has a data type limitation 
		//switch cannot work with double float long boolean
		//switch has an operators limitation || && cannot use logical operators
		//cannot use relational operation
		/*double price= 10;
	*	switch(price) { -> cannot switch on a value of type double}
	 * 
	 */

	}

}
